# 版本更新说明换行显示设计

## 目标

版本更新弹窗按接口 `releaseNotes` 字符串中的换行符分行显示。

## 方案

在更新说明容器上增加 Tailwind `whitespace-pre-line`，保留换行并继续折叠普通空白。数据获取、文本内容和弹窗其他布局均不改变。

## 验证

使用包含 `\n` 的更新说明打开弹窗，确认每一项独占一行，滚动区域及主题样式保持不变。
